
This serves only as a proof that I invented this algorithm, in case anyone claims that he came up with a number vectorizer/unifier.
The code for it is in NumLab.js, from which stems the crypting mechanism found in uncrunchable.js. It converts a Uint32 array into a number of arbitrary-length, performs encryption, then converts the number back to a UInt32 array
NumLab was first published to GitHub on July 24th 2026.
